import Product from "../entities/Product";

export default function sortData(products: Product[]) {

    // function sortByKey(key: string) {

    // }




    // products.sort(function sortByName (a: Product, b: Product) : any {
    //     return a.name > b.name;
    // })

    // products.sort(function sortByCategory (a: Product, b: Product) : any {
    //     return a.category > b.category;
    // })

    // products.sort(function sortById (a: Product, b: Product) : any {
    //     return parseFloat(a.price) > parseFloat(b.price);
    // })
}
